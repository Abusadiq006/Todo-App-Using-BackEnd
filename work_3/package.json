{
  "name": "work_3",
  "version": "1.0.0",
  "description": "A full stack todo application that uses a NodeJS backend, a SQLite database and JWT Authentication.",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --experimental-script-types --experimental-sqlite ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./src/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Full stack todo application that uses a NodeJSbackend, a SQLite database and JWT Authentication.",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  }
}
